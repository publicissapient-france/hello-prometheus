global:
  scrape_interval: 10s

# A list of scrape configurations.
scrape_configs:
  - job_name: xblagues
    static_configs:
      - targets:
          - 'localhost:8080'
        labels:
          source: "xblagues"
